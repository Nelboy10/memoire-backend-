# Django
SECRET_KEY=your-secret-key-here
DEBUG=True

# Database (optional — leave blank to use local SQLite)
DATABASE_URL=

# Email (optional — in DEBUG mode, emails are printed to console)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@example.com
